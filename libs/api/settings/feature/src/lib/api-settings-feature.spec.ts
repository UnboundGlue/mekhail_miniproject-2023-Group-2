import { apiSettingsFeature } from './api-settings-feature';

describe('apiSettingsFeature', () => {
  it('should work', () => {
    expect(apiSettingsFeature()).toEqual('api-settings-feature');
  });
});
